# neuthek development defaults. Do not put production secrets in .env.
# Note: the literal default values `istore` / `istore-*` for Postgres
# credentials and MinIO bucket names are intentionally preserved from
# the pre-rename era so existing local data stays accessible. Change
# these per-environment as you like; the names have no user-visible
# meaning.
APP_ENV=dev
FRONTEND_BASE_URL=http://localhost:5173

POSTGRES_USER=istore
POSTGRES_PASSWORD=istore
POSTGRES_DB=istore
DATABASE_URL=postgresql+asyncpg://istore:istore@localhost:5432/istore
DATABASE_URL_SYNC=postgresql+psycopg2://istore:istore@localhost:5432/istore
POSTGRES_AT_REST_ENCRYPTION=

REDIS_URL=redis://localhost:6379/0

MINIO_ENDPOINT=localhost:9000
MINIO_ACCESS_KEY=istore
MINIO_SECRET_KEY=istorepass
MINIO_SECURE=false
MINIO_BUCKET_ORIGINALS=istore-originals
MINIO_BUCKET_SERVED=istore-served
MINIO_BUCKET_FACES=istore-faces
MINIO_BUCKET_QUARANTINE=istore-quarantine
MINIO_SSE_MODE=off
MINIO_SSE_KMS_KEY_ID_CONTENT=
MINIO_SSE_KMS_KEY_ID_BIOMETRIC=

JWT_SECRET=dev-only-jwt-secret-CHANGE-IN-PROD
JWT_LIFETIME_SECONDS=86400
SECRET_MANAGER=env_file

UPLOAD_MAX_BYTES=209715200
UPLOAD_MAX_COUNT_PER_HOUR=300
UPLOAD_MAX_BYTES_PER_DAY=10737418240
UPLOAD_MAX_IMAGE_PIXELS=120000000
DOWNLOAD_URL_TTL_SECONDS=300
REQUIRE_SIGNED_DOWNLOADS=false
SECURITY_RATE_LIMITS_ENABLED=true
AUTH_RATE_LIMIT_PER_MINUTE=5
AUTH_LOCKOUT_FAILURES=5

BACKUP_AGE_RECIPIENT=

SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=neuthek <noreply@neuthek.local>

CLOUD_ENCRYPTION_KEY=
GOOGLE_OAUTH_CLIENT_ID=
GOOGLE_OAUTH_CLIENT_SECRET=
GOOGLE_OAUTH_REDIRECT_URI=http://localhost:8000/cloud/callback/google_drive
